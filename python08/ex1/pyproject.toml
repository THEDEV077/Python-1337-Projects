[tool.poetry]
name = "matrix-project"
version = "0.1.0"
description = ""
authors = ["yahya"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
pandas = "*"
numpy = "*"
matplotlib = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
